name: CI/CD Pipeline

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona la rama correcta (feature o develop)
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      # 2️⃣ Configura .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # 🔍 Debug paths (opcional)
      - name: Debug paths
        run: |
          pwd
          ls -R ${{ github.workspace }}

      # 3️⃣ Restaurar dependencias de toda la solución
      - name: Restore backend dependencies
        run: dotnet restore ${{ github.workspace }}/Auth.sln

      # 4️⃣ Compilar toda la solución
      - name: Build backend
        run: dotnet build ${{ github.workspace }}/Auth.sln --configuration Release --no-restore

      # 5️⃣ Ejecutar tests de toda la solución
      - name: Run backend tests
        run: dotnet test ${{ github.workspace }}/Auth.sln --no-build --verbosity normal

      # 6️⃣ Instalar dependencias frontend
      - name: Install frontend dependencies
        working-directory: ${{ github.workspace }}/frontend
        run: npm install

      # 7️⃣ Build frontend
      - name: Build frontend
        working-directory: ${{ github.workspace }}/frontend
        run: npm run build

      # 8️⃣ Publicar backend (empaquetar artefacto final)
      - name: Publish backend
        run: dotnet publish ${{ github.workspace }}/backend/Web.API/Web.API.csproj -c Release -o ${{ github.workspace }}/publish/backend

      # 9️⃣ Guardar artefactos
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-artifacts
          path: ${{ github.workspace }}/publish

  # 🚀 Deploy por FTP (solo en PR a main)
  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref == 'main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: app-artifacts
          path: ${{ github.workspace }}/publish

      # 💾 Despliegue FTP (descomentá para usarlo)
      # - name: Deploy via FTP
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      #   with:
      #     server: ${{ secrets.FTP_HOST }}
      #     username: ${{ secrets.FTP_USER }}
      #     password: ${{ secrets.FTP_PASS }}
      #     local-dir: ${{ github.workspace }}/publish/backend
      #     server-dir: /path/to/your/site/
